name: Deploy FinMA

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    name: Deploy Frontend to Vercel
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Copy bot data into public folder for Vercel
        run: |
          mkdir -p frontend/public/data/latest
          if [ -d "transfer/latest" ]; then
            cp -r transfer/latest/. frontend/public/data/latest/
            echo "Bot data copied from transfer/latest to public/data/latest"
          else
            echo "No transfer/latest found — committed public/data/latest will be used"
          fi
          echo "Files in public/data/latest: $(ls frontend/public/data/latest | wc -l)"

      - name: Build
        working-directory: frontend
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
          NEXT_PUBLIC_DATA_URL: /data/latest

      - name: Deploy to Vercel
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: frontend
          vercel-args: '--prod'
