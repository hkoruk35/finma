// Anonim ziyaretçi artık üyelerle aynı gerçek Copilot sohbetini kullanıyor
// (bkz. CopilotDrawer.tsx, Faz 2 — NVDA-özel/kampanya scriptli demo akışı
// kaldırıldı). Bu dosya sadece SupportedLocale tipini barındırmak için
// kalıyor — hâlâ CopilotDrawer.tsx ve api/copilot/tasks/route.ts tarafından
// kullanılıyor.

export type SupportedLocale = "tr" | "en" | "pt" | "es" | "fr" | "id";
